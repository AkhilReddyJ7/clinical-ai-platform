services:
  api:
    build:
      context: .
      dockerfile: infrastructure/docker/api.Dockerfile

    ports:
      - "8000:8000"

    volumes:
      - .:/app

    command: >
      uv run uvicorn apps.api.main:app
      --host 0.0.0.0
      --port 8000
      --reload
